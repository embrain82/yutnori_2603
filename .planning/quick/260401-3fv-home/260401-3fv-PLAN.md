# Quick Task 260401-3fv Plan

**Date:** 2026-04-01
**Task:** 게임판 출발 글자 위치를 이동하고, 턴 전환 중복 이벤트를 막고, 시작 시 두 말 모두 HOME일 때도 자동 이동되게 수정
**Mode:** `gsd-quick`

## Tasks

1. Move the persistent board start label so it no longer overlaps the first path station.
2. Tighten the AI turn handoff timer so the same `aiThinking` window does not reschedule duplicate turn events.
3. Expand auto-move resolution so equivalent opening moves from HOME also resolve immediately, while branch choices remain selectable.

## Verification

- Update store tests for equivalent HOME moves and revised auto-move expectations.
- Add a Game timer regression test for callback churn during `aiThinking`.
- Run `npm test`, targeted `eslint`, and `npm run build`.
