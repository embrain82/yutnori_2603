---
phase: 05-game-screens-integration
plan: 03
type: execute
wave: 3
depends_on:
  - "05-02"
files_modified:
  - src/lib/yut/ai.ts
  - src/lib/yut/__tests__/ai.test.ts
  - src/store/gameStore.ts
  - src/store/__tests__/gameStore.test.ts
  - src/hooks/usePostMessage.ts
  - src/hooks/__tests__/usePostMessage.test.tsx
  - src/components/Game.tsx
  - src/components/__tests__/Game.test.tsx
  - src/app/page.tsx
autonomous: true
requirements:
  - VIS-02
  - VIS-03
  - VIS-04
  - INTG-01
  - INTG-02
  - INTG-03

must_haves:
  truths:
    - "AI turns visibly pause in an `aiThinking` state before each AI throw"
    - "AI move selection uses the same candidate model as the player instead of bypassing Phase 3/4 UI seams"
    - "Victory and defeat finalize the session exactly once before bridge messages are emitted"
    - "The final app entry renders the real `Game` component rather than the temporary Phase 4 throw demo"
  artifacts:
    - path: "src/lib/yut/ai.ts"
      provides: "AI candidate selection that can work with branch-aware move candidates"
      exports: ["selectAiCandidate", "AI_CONFIG", "DEFAULT_AI_WEIGHTS"]
    - path: "src/hooks/usePostMessage.ts"
      provides: "Typed inbound coupon config plus outbound start/end/win bridge messages"
      exports: ["usePostMessage"]
    - path: "src/app/page.tsx"
      provides: "Client-safe page entry for the final game shell"
      exports: ["default"]
  key_links:
    - from: "src/store/gameStore.ts"
      to: "src/lib/yut/ai.ts"
      via: "AI chooses one UI-ready candidate during its turn"
      pattern: "selectAiCandidate"
    - from: "src/hooks/usePostMessage.ts"
      to: "src/lib/yut/session.ts"
      via: "uses typed Yut bridge messages and coupon config types"
      pattern: "YUT_GAME_WIN"
    - from: "src/components/Game.tsx"
      to: "src/hooks/usePostMessage.ts"
      via: "wires bridge lifecycle into the real app shell"
      pattern: "usePostMessage"
---

<objective>
Finish the playable loop by wiring AI turn pacing, WebView messaging, and the final page entry.

Purpose: This plan turns the Phase 5 screen shell into the actual product flow. It makes AI turns readable, finalizes sessions on win/loss, emits lifecycle messages to the parent WebView, and swaps the page entry from the temporary throw demo to the real game.

Output: AI candidate selection in `ai.ts`, store updates for AI turn pacing and finalization, `usePostMessage.ts`, `Game.tsx` timer/bridge wiring, and updated `src/app/page.tsx`.
</objective>

<execution_context>
@/Users/nonsleeper/.codex/get-shit-done/workflows/execute-plan.md
@/Users/nonsleeper/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-game-screens-integration/05-RESEARCH.md
@.planning/phases/05-game-screens-integration/05-VALIDATION.md

<interfaces>
From src/store/gameStore.ts (05-01 / 05-02):
```typescript
startThrow(result: ThrowResult): void
finishThrowReveal(): void
completeAnimation(): void
restartGame(): void
```

Bridge payloads from src/lib/yut/session.ts:
```typescript
export interface YutCouponConfigMessage { type: 'YUT_COUPON_CONFIG'; ... }
export interface YutGameStartMessage { type: 'YUT_GAME_START'; payload: { sessionId: string; startedAt: string } }
export interface YutGameEndMessage { type: 'YUT_GAME_END'; payload: { sessionId: string; completedAt: string; winner: Team | null } }
export interface YutGameWinMessage { type: 'YUT_GAME_WIN'; payload: GameSessionPayload }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend AI and store orchestration for visible AI turns and finalized end states</name>
  <files>src/lib/yut/ai.ts, src/lib/yut/__tests__/ai.test.ts, src/store/gameStore.ts, src/store/__tests__/gameStore.test.ts</files>
  <read_first>
    - src/lib/yut/ai.ts (current scorer and easy-AI behavior)
    - src/lib/yut/moveCandidates.ts (05-01 candidate contract)
    - src/store/gameStore.ts (05-01 store actions and state)
    - src/components/screens/PlayScreen.tsx (05-02 expected AI UI states)
    - .planning/phases/02-capture-stacking-ai/02-RESEARCH.md (AI chaining and capture-extra-throw behavior)
    - .planning/phases/05-game-screens-integration/05-RESEARCH.md (Pattern 4)
  </read_first>
  <behavior>
    - Test 1: `selectAiCandidate()` returns one of the provided move candidates and respects `AI_CONFIG.randomMoveRate` / `captureIgnoreRate`
    - Test 2: when AI is in `aiThinking`, `runAiTurn()` starts a throw and sets `phase === 'throwing'`
    - Test 3: after AI throw reveal with no extra throws remaining, the store auto-selects an AI candidate instead of entering player-style manual selection
    - Test 4: completing an AI animation with no more queued work switches back to the player with `phase === 'readyToThrow'`
    - Test 5: when both player pieces finish, the store finalizes the session and sets `phase === 'victory'`
    - Test 6: AI reaction becomes `excited` on capture or finish, `worried` when the player is near victory, and `smug` otherwise
  </behavior>
  <action>
    **RED phase:** extend `src/lib/yut/__tests__/ai.test.ts` and `src/store/__tests__/gameStore.test.ts` with the six tests above. Run:
    - `npx vitest run src/lib/yut/__tests__/ai.test.ts -x`
    - `npx vitest run src/store/__tests__/gameStore.test.ts -x`
    Confirm failures first.

    **GREEN phase:** update `src/lib/yut/ai.ts` to add:
    - `export function selectAiCandidate(candidates: MoveCandidate[], pieces: PieceState[], team: Team, weights: AiWeights = DEFAULT_AI_WEIGHTS): MoveCandidate | null`

    `selectAiCandidate()` must:
    - score each candidate with `evaluateMove()`
    - keep the existing stochastic behavior:
      - random pick when `Math.random() < AI_CONFIG.randomMoveRate`
      - otherwise filter capture moves out when `Math.random() < AI_CONFIG.captureIgnoreRate` and any non-capture options exist
      - otherwise return the highest-scored candidate

    Update `src/store/gameStore.ts` with these exact additions:
    - `runAiTurn(): void`
    - internal helper that derives AI reaction:
      - `excited` when AI captures or finishes a piece
      - `worried` when any player leader piece is on station `>= 15`
      - `smug` otherwise
    - `runAiTurn()` behavior:
      - only act when `turnState.activeTeam === 'ai'`
      - call `startThrow(generateThrow())`
    - `finishThrowReveal()` behavior for AI:
      - if more throws remain, return to `aiThinking`
      - once `activeMove` and `moveCandidates` are ready, call `selectAiCandidate()` and immediately resolve that candidate into `pendingAnimation`
    - `completeAnimation()` behavior:
      - on victory/defeat, finalize the session with `finalizeSession(session, winner, new Date())`
      - if AI has more queued throws, return to `aiThinking`
      - if queued moves remain, keep draining them
      - if turn ends, switch to player with `phase = 'readyToThrow'` and `aiReaction = 'neutral'`

    Add one short comment above the AI auto-selection branch explaining that AI still consumes the same queue/candidate model as the player, but without a manual tap step.

    Run both test files again and make them pass.
  </action>
  <verify>
    <automated>npx vitest run src/lib/yut/__tests__/ai.test.ts -x</automated>
    <automated>npx vitest run src/store/__tests__/gameStore.test.ts -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep 'selectAiCandidate' src/lib/yut/ai.ts` returns a match
    - `grep 'runAiTurn' src/store/gameStore.ts` returns a match
    - `grep "aiReaction: 'neutral'" src/store/gameStore.ts` returns a match
    - `grep 'finalizeSession' src/store/gameStore.ts` returns a match
    - `npx vitest run src/lib/yut/__tests__/ai.test.ts -x` passes with 0 failures
    - `npx vitest run src/store/__tests__/gameStore.test.ts -x` passes with 0 failures
  </acceptance_criteria>
  <done>The store now drives a readable AI turn loop with visible thinking, emoji reactions, and finalized end states that are ready for bridge emission.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the WebView bridge hook and swap the page entry to the real game</name>
  <files>src/hooks/usePostMessage.ts, src/hooks/__tests__/usePostMessage.test.tsx, src/components/Game.tsx, src/components/__tests__/Game.test.tsx, src/app/page.tsx</files>
  <read_first>
    - 260330_rps/src/hooks/usePostMessage.ts (proven inbound/outbound bridge pattern)
    - 260330_rps/src/hooks/__tests__/usePostMessage.test.ts (test strategy)
    - src/lib/yut/session.ts (05-01 message contracts)
    - src/store/gameStore.ts (Task 1 final phase and session semantics)
    - src/components/Game.tsx (05-02 screen orchestration)
    - src/app/page.tsx (current Phase 4 throw demo entry)
    - next.config.ts (iframe headers already in place)
    - .planning/phases/05-game-screens-integration/05-RESEARCH.md (Pattern 5)
  </read_first>
  <behavior>
    - Test 1: inbound `YUT_COUPON_CONFIG` from an allowed origin stores coupon config in the store
    - Test 2: `YUT_GAME_START` is posted exactly once when gameplay begins and the session has `startedAt`
    - Test 3: `YUT_GAME_END` is posted exactly once when the store reaches `victory` or `defeat` with a finalized session
    - Test 4: `YUT_GAME_WIN` is posted exactly once on victory with the finalized session payload
    - Test 5: `Game` calls `runAiTurn()` after `900ms` when `phase === 'aiThinking'`
    - Test 6: `src/app/page.tsx` renders the real `Game` component instead of `ThrowDemo`
  </behavior>
  <action>
    **RED phase:** create `src/hooks/__tests__/usePostMessage.test.tsx` and extend `src/components/__tests__/Game.test.tsx` for the AI timer case. Use fake timers for the `900ms` delay and spy on `window.parent.postMessage`. Run:
    - `npx vitest run src/hooks/__tests__/usePostMessage.test.tsx -x`
    - `npx vitest run src/components/__tests__/Game.test.tsx -x`
    Confirm failures first.

    **GREEN phase:** create `src/hooks/usePostMessage.ts` with:
    - `'use client'`
    - store selectors for `phase`, `session`, and `setCouponConfig`
    - `const targetOrigin = process.env.NEXT_PUBLIC_ALLOWED_ORIGIN || '*'`
    - inbound listener for `YUT_COUPON_CONFIG`
    - origin guard: `if (targetOrigin !== '*' && event.origin !== targetOrigin) return`
    - safe browser guard: if `window.parent === window`, receiving still works but outbound sends should no-op
    - once-only refs for start, end, and win sends

    Exact outbound behavior:
    - send `YUT_GAME_START` when phase leaves `idle`, `session.startedAt` exists, and start has not been sent
    - send `YUT_GAME_END` when `phase === 'victory' || phase === 'defeat'`, `session.completedAt` exists, and end has not been sent
    - send `YUT_GAME_WIN` only when `phase === 'victory'`, `session.completedAt` exists, and win has not been sent
    - reset the once-only refs on `restartGame()` by clearing them whenever phase returns to `idle`

    Update `src/components/Game.tsx` to:
    - call `usePostMessage()`
    - keep a `timerRef`
    - when `phase === 'aiThinking'`, schedule `runAiTurn()` after exactly `900ms`
    - clear the timer on phase change/unmount

    Update `src/app/page.tsx` to:
    - remove `'use client'`
    - import `dynamic` from `next/dynamic`
    - declare:
      ```typescript
      const Game = dynamic(() => import('@/components/Game'), {
        ssr: false,
        loading: () => <main className="min-h-dvh bg-[#FFF8EE]" />,
      })
      ```
    - export default page that renders `<Game />`

    Add one short comment above the `aiThinking` timer effect explaining that the delay is intentionally UI-owned so the store can stay deterministic and synchronous under test.

    Run both test files again and make them pass.
  </action>
  <verify>
    <automated>npx vitest run src/hooks/__tests__/usePostMessage.test.tsx -x</automated>
    <automated>npx vitest run src/components/__tests__/Game.test.tsx -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep "type: 'YUT_COUPON_CONFIG'" src/hooks/usePostMessage.ts` returns a match
    - `grep "type: 'YUT_GAME_START'" src/hooks/usePostMessage.ts` returns a match
    - `grep "type: 'YUT_GAME_END'" src/hooks/usePostMessage.ts` returns a match
    - `grep "type: 'YUT_GAME_WIN'" src/hooks/usePostMessage.ts` returns a match
    - `grep '900' src/components/Game.tsx` returns a match
    - `grep 'dynamic(() => import(' src/app/page.tsx` returns a match
    - `grep '@/components/Game' src/app/page.tsx` returns a match
    - `npx vitest run src/hooks/__tests__/usePostMessage.test.tsx -x` passes with 0 failures
    - `npx vitest run src/components/__tests__/Game.test.tsx -x` passes with 0 failures
  </acceptance_criteria>
  <done>The final app shell now talks to the parent WebView, paces AI turns visibly, and loads the real Game component from the page entry.</done>
</task>

</tasks>

<verification>
1. `npx vitest run src/lib/yut/__tests__/ai.test.ts -x` — AI candidate selection tests pass
2. `npx vitest run src/store/__tests__/gameStore.test.ts -x` — store AI orchestration tests pass
3. `npx vitest run src/hooks/__tests__/usePostMessage.test.tsx -x` — bridge hook tests pass
4. `npx vitest run src/components/__tests__/Game.test.tsx -x` — AI delay and screen orchestration tests pass
5. `npm test` — full suite remains green after full integration wiring
6. Manual browser/WebView check: start a game, finish a loss, finish a win, and confirm lifecycle + victory bridge messages in the parent
</verification>

<success_criteria>
- AI turns visibly show a thinking state before acting
- AI reactions change contextually without bypassing the board/overlay flow
- Sessions finalize exactly once on victory/defeat
- The app sends `YUT_GAME_START`, `YUT_GAME_END`, and `YUT_GAME_WIN` at the right times
- `src/app/page.tsx` renders the real game instead of the temporary Phase 4 demo
</success_criteria>
