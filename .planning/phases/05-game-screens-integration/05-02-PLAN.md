---
phase: 05-game-screens-integration
plan: 02
type: execute
wave: 2
depends_on:
  - "05-01"
files_modified:
  - src/components/game/TurnBanner.tsx
  - src/components/game/AiReactionBubble.tsx
  - src/components/screens/PlayScreen.tsx
  - src/components/screens/__tests__/PlayScreen.test.tsx
  - src/components/screens/IdleScreen.tsx
  - src/components/screens/ResultScreen.tsx
  - src/components/screens/__tests__/ResultScreen.test.tsx
  - src/components/Game.tsx
  - src/components/__tests__/Game.test.tsx
autonomous: true
requirements:
  - VIS-02
  - VIS-04

must_haves:
  truths:
    - "The active play screen keeps the board visible while layering turn status, HOME zones, and the Phase 4 throw overlay on top"
    - "The current turn is readable at all times during gameplay"
    - "AI emotional state is visible through a lightweight emoji reaction bubble without replacing the board layout"
    - "Screen rendering stays phase-driven through one root Game component"
  artifacts:
    - path: "src/components/game/TurnBanner.tsx"
      provides: "Persistent current-turn indicator with remaining-throw context"
      exports: ["TurnBanner"]
    - path: "src/components/game/AiReactionBubble.tsx"
      provides: "Small emoji + status capsule for AI persona feedback"
      exports: ["AiReactionBubble", "AI_REACTION_EMOJIS"]
    - path: "src/components/screens/PlayScreen.tsx"
      provides: "Board-backed active game screen with overlay, controls, and interaction states"
      exports: ["PlayScreen"]
    - path: "src/components/Game.tsx"
      provides: "Phase-driven screen orchestrator"
      exports: ["default"]
  key_links:
    - from: "src/components/screens/PlayScreen.tsx"
      to: "src/store/gameStore.ts"
      via: "reads board, throw, selection, animation, and AI reaction state"
      pattern: "useGameStore"
    - from: "src/components/screens/PlayScreen.tsx"
      to: "src/components/throw/YutThrowOverlay.tsx"
      via: "reuses the Phase 4 overlay when phase === 'throwing'"
      pattern: "YutThrowOverlay"
    - from: "src/components/Game.tsx"
      to: "src/components/screens/IdleScreen.tsx"
      via: "phase-based screen rendering"
      pattern: "IdleScreen"
---

<objective>
Build the user-facing screen shell for the playable Yut Nori app.

Purpose: After the store foundation exists, the next step is to expose it through actual screen components: an entry screen, an active play screen that keeps the board centered, and a result screen. This plan also introduces the persistent turn banner and AI reaction bubble required for the game's readable personality.

Output: `TurnBanner.tsx`, `AiReactionBubble.tsx`, `PlayScreen.tsx`, `IdleScreen.tsx`, `ResultScreen.tsx`, `Game.tsx`, and screen tests.
</objective>

<execution_context>
@/Users/nonsleeper/.codex/get-shit-done/workflows/execute-plan.md
@/Users/nonsleeper/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-game-screens-integration/05-RESEARCH.md
@.planning/phases/05-game-screens-integration/05-VALIDATION.md

<interfaces>
From src/store/gameStore.ts (created in 05-01):
```typescript
export type GamePhase =
  | 'idle'
  | 'readyToThrow'
  | 'throwing'
  | 'selectingPiece'
  | 'animatingMove'
  | 'confirmingStack'
  | 'aiThinking'
  | 'victory'
  | 'defeat'
```

Existing render layers:
```typescript
export function Board(props?: {
  pieces?: PieceState[]
  selectedPieceId?: string | null
  validDestinations?: Array<{
    stationId: number
    isBranchShortcut: boolean
    isBranchContinue: boolean
  }>
  ...
}): React.JSX.Element

export function HomeZone(props: {
  playerHomePieces: HomePiece[]
  aiHomePieces: HomePiece[]
  selectablePieceIds: string[]
  selectedPieceId: string | null
  onPieceSelect: (pieceId: string) => void
}): React.JSX.Element
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create the active play shell with turn banner, AI reaction, and board controls</name>
  <files>src/components/game/TurnBanner.tsx, src/components/game/AiReactionBubble.tsx, src/components/screens/PlayScreen.tsx, src/components/screens/__tests__/PlayScreen.test.tsx</files>
  <read_first>
    - src/store/gameStore.ts (05-01 state and action names)
    - src/components/board/Board.tsx (board prop contract)
    - src/components/board/HomeZone.tsx (HOME area contract)
    - src/components/throw/YutThrowOverlay.tsx (overlay props)
    - src/components/throw/ThrowResultCard.tsx (Korean result labels)
    - src/hooks/useHopAnimation.ts (board movement and shake helpers)
    - src/components/throw/ThrowDemo.tsx (current board-centered layout baseline)
    - .planning/phases/05-game-screens-integration/05-RESEARCH.md (Patterns 2-4)
  </read_first>
  <behavior>
    - Test 1: the play screen renders `내 차례` when `activeTeam === 'player'`
    - Test 2: the play screen renders `AI 차례` and `생각 중...` when `phase === 'aiThinking'`
    - Test 3: the AI reaction bubble renders `😄`, `😟`, `😏`, and `🙂` from the exact reaction mapping
    - Test 4: the player throw button labeled `윷 던지기` is visible only when `phase === 'readyToThrow'` and `activeTeam === 'player'`
    - Test 5: selecting a piece routes through `onPieceSelect` by calling the store's `selectPiece`
    - Test 6: the play screen renders both `HomeZone` and `Board`
    - Test 7: the stack prompt renders `함께 가기` and `따로 가기` when `phase === 'confirmingStack'`
  </behavior>
  <action>
    **RED phase:** create `src/components/screens/__tests__/PlayScreen.test.tsx` and mock `@/store/gameStore` selectors plus `@/lib/yut/throw` where needed. Run `npx vitest run src/components/screens/__tests__/PlayScreen.test.tsx -x` and confirm failure first.

    **GREEN phase:** create `src/components/game/TurnBanner.tsx` with:
    - `'use client'`
    - props `{ activeTeam: Team; throwsRemaining: number }`
    - label text:
      - player -> `내 차례`
      - ai -> `AI 차례`
    - subtext: `남은 던지기 ${throwsRemaining}`
    - root classes: `flex items-center justify-between rounded-full bg-white/80 px-4 py-2 text-[#1A1A2E] shadow-[0_10px_30px_rgba(120,75,24,0.12)]`

    Create `src/components/game/AiReactionBubble.tsx` with:
    - `'use client'`
    - `export const AI_REACTION_EMOJIS = { neutral: '🙂', excited: '😄', worried: '😟', smug: '😏' } as const`
    - props `{ reaction: AiReaction; thinking: boolean }`
    - render `생각 중...` when `thinking` is true, otherwise `AI 반응`
    - emoji bubble classes: `rounded-full bg-[#FFF7E2] px-3 py-2 text-2xl shadow-[0_8px_24px_rgba(120,75,24,0.12)]`

    Create `src/components/screens/PlayScreen.tsx` with:
    - `'use client'`
    - store selectors for:
      - `phase`, `turnState.activeTeam`, `turnState.throwsRemaining`
      - `pieces`, `selectedPieceId`, `validDestinations`, `activeThrow`, `pendingStack`, `aiReaction`
      - `startThrow`, `finishThrowReveal`, `selectPiece`, `selectDestination`, `resolveStack`
    - local board-area layout:
      - top: `<TurnBanner />`
      - below: `<HomeZone />`
      - center: board wrapper with `<Board />` underneath and `<YutThrowOverlay />` above
      - floating corner: `<AiReactionBubble />`
    - player throw button behavior:
      - label `윷 던지기`
      - on click: call `startThrow(generateThrow())`
      - render only when `phase === 'readyToThrow' && activeTeam === 'player'`
    - overlay wiring:
      - `<YutThrowOverlay open={phase === 'throwing'} result={activeThrow} onComplete={finishThrowReveal} />`
    - board wiring:
      - pass `pieces`, `selectedPieceId`, and `validDestinations`
      - `onPieceSelect={selectPiece}`
      - `onDestinationSelect={selectDestination}`
    - derive HOME pieces from `pieces` where `position.station === HOME`
    - derive `selectablePieceIds` from current `moveCandidates`
    - render stack prompt card only when `phase === 'confirmingStack' && pendingStack !== null`
    - button copy inside stack prompt:
      - confirm -> `함께 가기`
      - decline -> `따로 가기`

    Add one short comment above the derived `selectablePieceIds` calculation explaining that the board and HOME zones share the same piece-selection source of truth.

    Run the play-screen test file and make it pass.
  </action>
  <verify>
    <automated>npx vitest run src/components/screens/__tests__/PlayScreen.test.tsx -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep '내 차례' src/components/game/TurnBanner.tsx` returns a match
    - `grep 'AI 차례' src/components/game/TurnBanner.tsx` returns a match
    - `grep "AI_REACTION_EMOJIS = { neutral: '🙂', excited: '😄', worried: '😟', smug: '😏' }" src/components/game/AiReactionBubble.tsx` returns a match
    - `grep '생각 중...' src/components/game/AiReactionBubble.tsx` returns a match
    - `grep '윷 던지기' src/components/screens/PlayScreen.tsx` returns a match
    - `grep 'YutThrowOverlay' src/components/screens/PlayScreen.tsx` returns a match
    - `grep '함께 가기' src/components/screens/PlayScreen.tsx` returns a match
    - `npx vitest run src/components/screens/__tests__/PlayScreen.test.tsx -x` passes with 0 failures
  </acceptance_criteria>
  <done>The game now has a board-backed active play screen with persistent turn status, AI personality feedback, direct board controls, and the existing throw overlay wired in.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create the idle/result screens and phase-driven Game orchestrator</name>
  <files>src/components/screens/IdleScreen.tsx, src/components/screens/ResultScreen.tsx, src/components/screens/__tests__/ResultScreen.test.tsx, src/components/Game.tsx, src/components/__tests__/Game.test.tsx</files>
  <read_first>
    - 260330_rps/src/components/Game.tsx (screen orchestration pattern)
    - 260330_rps/src/components/screens/ResultScreen.tsx (victory/defeat copy and coupon area pattern)
    - src/store/gameStore.ts (05-01 phases and actions)
    - src/components/screens/PlayScreen.tsx (Task 1 actual export)
    - src/app/layout.tsx (metadata and page shell baseline)
    - .planning/phases/05-game-screens-integration/05-RESEARCH.md (Pattern 5 and technical decision 2)
  </read_first>
  <behavior>
    - Test 1: `IdleScreen` renders the `게임 시작` CTA and calls `startGame` when clicked
    - Test 2: `ResultScreen` renders `축하합니다!` when `phase === 'victory'` and `아쉽네요...` when `phase === 'defeat'`
    - Test 3: `ResultScreen` renders the coupon area when `couponConfig` exists on victory
    - Test 4: `Game` renders `IdleScreen` for `phase === 'idle'`
    - Test 5: `Game` renders `PlayScreen` for all active gameplay phases
    - Test 6: `Game` renders `ResultScreen` for `victory` and `defeat`
  </behavior>
  <action>
    **RED phase:** create `src/components/screens/__tests__/ResultScreen.test.tsx` and `src/components/__tests__/Game.test.tsx`. Mock `@/store/gameStore` so the tests can switch phases without touching real state. Run `npx vitest run src/components/screens/__tests__/ResultScreen.test.tsx src/components/__tests__/Game.test.tsx -x` and confirm failure first.

    **GREEN phase:** create `src/components/screens/IdleScreen.tsx` with:
    - `'use client'`
    - heading `윷놀이 한 판`
    - supporting copy: `3D 윷 던지기와 2D 말판으로 AI와 대결해보세요.`
    - CTA button text `게임 시작`
    - button calls `useGameStore((s) => s.startGame)`

    Create `src/components/screens/ResultScreen.tsx` with:
    - `'use client'`
    - phase-based heading:
      - victory -> `축하합니다!`
      - defeat -> `아쉽네요...`
    - reuse `couponConfig` from the store
    - when `phase === 'victory' && couponConfig`:
      - render `data-testid="coupon-area"`
      - show optional image, coupon code, and coupon text
    - fallback victory copy when no config: `쿠폰은 이벤트 페이지에서 확인하세요`
    - retry button text:
      - victory -> `한 번 더 하기`
      - defeat -> `다시 하기`
      - calls `restartGame`

    Create `src/components/Game.tsx` with:
    - `'use client'`
    - `AnimatePresence` from `motion/react`
    - render:
      - `IdleScreen` when `phase === 'idle'`
      - `PlayScreen` when phase is one of `readyToThrow`, `throwing`, `selectingPiece`, `animatingMove`, `confirmingStack`, `aiThinking`
      - `ResultScreen` when phase is `victory` or `defeat`
    - keep the root wrapper `relative min-h-dvh overflow-hidden [contain:content]`

    Add one short comment above the gameplay-phase array explaining that the play shell stays mounted across intra-turn states so the board does not remount between throw, selection, and animation.

    Run the result-screen and Game test files and make them pass.
  </action>
  <verify>
    <automated>npx vitest run src/components/screens/__tests__/ResultScreen.test.tsx src/components/__tests__/Game.test.tsx -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep '게임 시작' src/components/screens/IdleScreen.tsx` returns a match
    - `grep '축하합니다!' src/components/screens/ResultScreen.tsx` returns a match
    - `grep '아쉽네요...' src/components/screens/ResultScreen.tsx` returns a match
    - `grep 'coupon-area' src/components/screens/ResultScreen.tsx` returns a match
    - `grep 'PlayScreen' src/components/Game.tsx` returns a match
    - `grep 'IdleScreen' src/components/Game.tsx` returns a match
    - `grep 'ResultScreen' src/components/Game.tsx` returns a match
    - `npx vitest run src/components/screens/__tests__/ResultScreen.test.tsx src/components/__tests__/Game.test.tsx -x` passes with 0 failures
  </acceptance_criteria>
  <done>The project has a complete screen shell: a start screen, an active play shell, a result screen, and one phase-driven Game component to switch between them.</done>
</task>

</tasks>

<verification>
1. `npx vitest run src/components/screens/__tests__/PlayScreen.test.tsx -x` — play-screen interaction and visibility tests pass
2. `npx vitest run src/components/screens/__tests__/ResultScreen.test.tsx src/components/__tests__/Game.test.tsx -x` — screen orchestration tests pass
3. `npm test` — full suite remains green after the new screen layer
</verification>

<success_criteria>
- The game has dedicated idle, play, and result screens
- The board stays visible as the center of the active play shell
- The current turn is readable at all times during active play
- AI emoji reactions are rendered through a small dedicated UI element
- All new screen tests pass
</success_criteria>
