---
phase: 04-3d-yut-throwing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - src/lib/yut/throwPose.ts
  - src/lib/yut/__tests__/throwPose.test.ts
  - src/lib/yut/index.ts
  - src/lib/throw3d/resourceTracker.ts
  - src/lib/throw3d/__tests__/resourceTracker.test.ts
autonomous: true
requirements:
  - THROW-03

must_haves:
  truths:
    - "Every ThrowResult maps deterministically to exactly four target stick faces matching do/gae/geol/yut/mo counts"
    - "Phase 4 adds three.js and cannon-es to package.json and package-lock.json with concrete versions"
    - "Tracked Three.js resources can be disposed and detached through one reusable helper before scene work begins"
  artifacts:
    - path: "src/lib/yut/throwPose.ts"
      provides: "Pure predetermined-result mapping from ThrowResult to four target stick poses"
      exports: ["YutStickFace", "YutStickTargetPose", "buildTargetPoses", "YUT_THROW_FACE_COUNTS"]
    - path: "src/lib/throw3d/resourceTracker.ts"
      provides: "Tracked cleanup helper for Object3D nodes, materials, textures, geometries, and renderer-owned disposables"
      exports: ["createResourceTracker"]
    - path: "src/lib/yut/index.ts"
      provides: "Barrel export for throw pose utilities"
      exports: ["buildTargetPoses"]
  key_links:
    - from: "src/lib/yut/throwPose.ts"
      to: "src/lib/yut/types.ts"
      via: "imports ThrowResult and ThrowName"
      pattern: "import.*ThrowResult.*from './types'"
    - from: "src/lib/throw3d/resourceTracker.ts"
      to: "src/lib/throw3d/createYutThrowScene.ts"
      via: "scene controller tracks meshes, materials, textures, renderer, and scene nodes"
      pattern: "createResourceTracker"
---

<objective>
Establish the pure result-mapping and cleanup foundations for the 3D throw overlay.

Purpose: Before touching WebGL or physics, lock down the deterministic contract that turns `ThrowResult` into four visible stick faces, add the required 3D dependencies, and create a reusable resource tracker that will guarantee GPU cleanup during repeated throw mounts/unmounts.

Output: `throwPose.ts` with deterministic target poses, `resourceTracker.ts` with explicit disposal behavior, updated package manifests for `three` and `cannon-es`, and focused unit tests.
</objective>

<execution_context>
@/Users/nonsleeper/.codex/get-shit-done/workflows/execute-plan.md
@/Users/nonsleeper/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-3d-yut-throwing/04-CONTEXT.md
@.planning/phases/04-3d-yut-throwing/04-RESEARCH.md
@.planning/phases/04-3d-yut-throwing/04-VALIDATION.md

<interfaces>
From src/lib/yut/types.ts:
```typescript
export type ThrowName = 'do' | 'gae' | 'geol' | 'yut' | 'mo'

export interface ThrowResult {
  name: ThrowName
  steps: number
  grantsExtra: boolean
}
```

From src/lib/yut/throw.ts:
```typescript
export function generateThrow(): ThrowResult
```

Locked context:
```text
- Overlay above board, not a page transition
- Stable 3/4 camera
- Warm wood casual tone
- Large centered Korean result label after settle
- Physics is visual-only; ThrowResult remains the source of truth
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Phase 4 dependencies and create deterministic throw pose mapping</name>
  <files>package.json, package-lock.json, src/lib/yut/throwPose.ts, src/lib/yut/__tests__/throwPose.test.ts, src/lib/yut/index.ts</files>
  <read_first>
    - package.json (current dependency set and scripts)
    - src/lib/yut/types.ts (ThrowResult and ThrowName contracts)
    - src/lib/yut/throw.ts (authoritative RNG result generation)
    - .planning/phases/04-3d-yut-throwing/04-CONTEXT.md (locked decisions D-01 through D-08)
    - .planning/phases/04-3d-yut-throwing/04-RESEARCH.md (Predetermined Result Mapping section)
  </read_first>
  <behavior>
    - Test 1: `buildTargetPoses({ name: 'do', ... })` returns four poses with exactly one `face === 'flat'`
    - Test 2: `buildTargetPoses({ name: 'gae', ... })` returns two flat and two round
    - Test 3: `buildTargetPoses({ name: 'geol', ... })` returns three flat and one round
    - Test 4: `buildTargetPoses({ name: 'yut', ... })` returns four flat
    - Test 5: `buildTargetPoses({ name: 'mo', ... })` returns four round
    - Test 6: every returned array has exactly four entries with numeric `yaw` and integer `slot`
    - Test 7: `YUT_THROW_FACE_COUNTS` exposes counts `{ do: 1, gae: 2, geol: 3, yut: 4, mo: 0 }`
    - Test 8: `src/lib/yut/index.ts` re-exports `buildTargetPoses`
  </behavior>
  <action>
    **Update dependencies first:** modify `package.json` and `package-lock.json` so dependencies include:
    - `"three": "^0.183.2"`
    - `"cannon-es": "^0.20.0"`

    Use `npm install three@^0.183.2 cannon-es@^0.20.0` so both manifest files update together.

    **RED phase:** create `src/lib/yut/__tests__/throwPose.test.ts` with the eight tests above. Construct ThrowResult fixtures using the exact shape:
    - `{ name: 'do', steps: 1, grantsExtra: false }`
    - `{ name: 'gae', steps: 2, grantsExtra: false }`
    - `{ name: 'geol', steps: 3, grantsExtra: false }`
    - `{ name: 'yut', steps: 4, grantsExtra: true }`
    - `{ name: 'mo', steps: 5, grantsExtra: true }`

    Run `npx vitest run src/lib/yut/__tests__/throwPose.test.ts -x` and confirm failures before implementation.

    **GREEN phase:** create `src/lib/yut/throwPose.ts` with:
    - `export type YutStickFace = 'flat' | 'round'`
    - `export interface YutStickTargetPose { face: YutStickFace; yaw: number; slot: 0 | 1 | 2 | 3 }`
    - `export const YUT_THROW_FACE_COUNTS: Record<ThrowName, number> = { do: 1, gae: 2, geol: 3, yut: 4, mo: 0 }`
    - `const BASE_YAWS = [-0.42, 0.28, -1.08, 0.92] as const`
    - `export function buildTargetPoses(result: ThrowResult): YutStickTargetPose[]` that:
      - reads `flatCount` from `YUT_THROW_FACE_COUNTS[result.name]`
      - returns four entries in slot order `0, 1, 2, 3`
      - assigns `face: 'flat'` to the first `flatCount` slots and `'round'` to the rest
      - assigns each entry the matching `yaw` from `BASE_YAWS`

    Add file-level documentation explaining that these poses represent the authoritative visual end-state after physics settle.

    **Update barrel export:** add `export * from './throwPose'` to `src/lib/yut/index.ts`.

    Run the test file again and get green.
  </action>
  <verify>
    <automated>npx vitest run src/lib/yut/__tests__/throwPose.test.ts -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep '"three": "\\^0.183.2"' package.json` returns a match
    - `grep '"cannon-es": "\\^0.20.0"' package.json` returns a match
    - `grep 'YUT_THROW_FACE_COUNTS' src/lib/yut/throwPose.ts` returns a match
    - `grep 'buildTargetPoses' src/lib/yut/index.ts` returns a match
    - `grep "type YutStickFace = 'flat' | 'round'" src/lib/yut/throwPose.ts` returns a match
    - `npx vitest run src/lib/yut/__tests__/throwPose.test.ts -x` passes with 0 failures
  </acceptance_criteria>
  <done>Phase 4 dependencies are installed, every ThrowResult maps to four deterministic target poses, and the mapping is exported from `@/lib/yut` with passing unit tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create explicit Three.js resource tracker for later scene cleanup</name>
  <files>src/lib/throw3d/resourceTracker.ts, src/lib/throw3d/__tests__/resourceTracker.test.ts</files>
  <read_first>
    - .planning/phases/04-3d-yut-throwing/04-RESEARCH.md (Explicit GPU/Physics Cleanup section)
    - src/hooks/useHopAnimation.ts (existing imperative cleanup pattern)
    - node_modules/three/src/core/Object3D.js (installed dependency shape, if available)
  </read_first>
  <behavior>
    - Test 1: tracked disposable objects with a `dispose()` method are disposed exactly once
    - Test 2: tracked `Object3D` children are removed from their parent before disposal completes
    - Test 3: tracked arrays of materials are fully iterated and disposed
    - Test 4: calling `dispose()` twice is safe and does not throw
  </behavior>
  <action>
    **RED phase:** create `src/lib/throw3d/__tests__/resourceTracker.test.ts` with the four tests above. Use simple fake disposables plus one real `Object3D` parent/child pair from `three` to verify parent detachment. Run `npx vitest run src/lib/throw3d/__tests__/resourceTracker.test.ts -x` and confirm failure first.

    **GREEN phase:** create `src/lib/throw3d/resourceTracker.ts` with:
    - `import { Object3D } from 'three'`
    - `type Trackable = Object3D | { dispose: () => void } | Array<{ dispose: () => void }>`
    - `export interface ResourceTracker { track<T extends Trackable>(resource: T): T; dispose(): void }`
    - `export function createResourceTracker(): ResourceTracker`

    Implement `track()` to store resources in a `Set`.

    Implement `dispose()` to:
    - iterate all tracked resources
    - if the resource is an array, call `dispose()` on every member
    - if the resource is an `Object3D` and `resource.parent` exists, call `resource.parent.remove(resource)`
    - if the resource has a `dispose()` method, call it
    - clear the internal `Set`

    Add a short comment above the `Object3D` removal branch explaining that scene nodes must be detached before renderer teardown to avoid stale references across repeated throw overlays.

    Run the test file and make it pass.
  </action>
  <verify>
    <automated>npx vitest run src/lib/throw3d/__tests__/resourceTracker.test.ts -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep 'createResourceTracker' src/lib/throw3d/resourceTracker.ts` returns a match
    - `grep 'resource.parent.remove(resource)' src/lib/throw3d/resourceTracker.ts` returns a match
    - `grep 'dispose()' src/lib/throw3d/resourceTracker.ts` returns at least 2 matches
    - `npx vitest run src/lib/throw3d/__tests__/resourceTracker.test.ts -x` passes with 0 failures
  </acceptance_criteria>
  <done>A reusable resource tracker exists for the Phase 4 scene controller, disposing tracked Three.js resources and detaching scene nodes safely under test.</done>
</task>

</tasks>

<verification>
1. `npx vitest run src/lib/yut/__tests__/throwPose.test.ts -x` — deterministic pose mapping tests pass
2. `npx vitest run src/lib/throw3d/__tests__/resourceTracker.test.ts -x` — disposal helper tests pass
3. `npm test` — full suite stays green after dependency and utility additions
</verification>

<success_criteria>
- `three` and `cannon-es` are added with exact semver ranges
- `buildTargetPoses()` deterministically converts ThrowResult into four stick poses
- `src/lib/yut/index.ts` exports the new pose utility
- Resource tracker disposes tracked objects and detaches Object3D children safely
- All new unit tests pass
</success_criteria>
