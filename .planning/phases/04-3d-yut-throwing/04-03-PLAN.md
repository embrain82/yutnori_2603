---
phase: 04-3d-yut-throwing
plan: 03
type: execute
wave: 3
depends_on:
  - "04-02"
files_modified:
  - src/components/throw/ThrowResultCard.tsx
  - src/components/throw/YutThrowOverlay.tsx
  - src/components/throw/ThrowDemo.tsx
  - src/components/throw/__tests__/YutThrowOverlay.test.tsx
  - src/components/throw/__tests__/ThrowDemo.test.tsx
  - src/app/page.tsx
autonomous: true
requirements:
  - THROW-04
  - THROW-05

must_haves:
  truths:
    - "The user can tap a visible `던지기` button to launch the overlay"
    - "The existing 2D board remains underneath the overlay, matching the locked layout decision"
    - "After settle, the overlay shows a centered large Korean result label for 도/개/걸/윷/모"
    - "The UI disables duplicate launches while a throw is active and allows a fresh throw after completion"
  artifacts:
    - path: "src/components/throw/ThrowResultCard.tsx"
      provides: "Large centered Korean result card component"
      exports: ["ThrowResultCard", "RESULT_LABELS"]
    - path: "src/components/throw/YutThrowOverlay.tsx"
      provides: "User-visible overlay shell around the scene hook and result reveal"
      exports: ["YutThrowOverlay"]
    - path: "src/components/throw/ThrowDemo.tsx"
      provides: "Temporary Phase 4 harness composed over the static board"
      exports: ["ThrowDemo"]
    - path: "src/app/page.tsx"
      provides: "Page entry point that renders the Phase 4 demo"
      exports: ["default"]
  key_links:
    - from: "src/components/throw/YutThrowOverlay.tsx"
      to: "src/hooks/useYutThrowScene.ts"
      via: "uses hook phase and reveal state to render canvas/result card"
      pattern: "useYutThrowScene"
    - from: "src/components/throw/ThrowDemo.tsx"
      to: "src/lib/yut/throw.ts"
      via: "calls generateThrow() when the button is pressed"
      pattern: "generateThrow"
    - from: "src/components/throw/ThrowDemo.tsx"
      to: "src/components/board/Board.tsx"
      via: "renders static board beneath the overlay"
      pattern: "<Board"
---

<objective>
Deliver the user-facing overlay, result reveal, and temporary demo harness for Phase 4.

Purpose: With the 3D controller in place, this plan exposes a real button-triggered flow: the user sees the board, taps `던지기`, watches the overlay animate, reads a large centered Korean result label, and can repeat the flow. This satisfies the visible product contract before Phase 5 wires the scene into full turn orchestration.

Output: `ThrowResultCard.tsx`, `YutThrowOverlay.tsx`, `ThrowDemo.tsx`, updated `src/app/page.tsx`, and interaction tests.
</objective>

<execution_context>
@/Users/nonsleeper/.codex/get-shit-done/workflows/execute-plan.md
@/Users/nonsleeper/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-3d-yut-throwing/04-CONTEXT.md
@.planning/phases/04-3d-yut-throwing/04-RESEARCH.md
@.planning/phases/04-3d-yut-throwing/04-VALIDATION.md

<interfaces>
From src/lib/yut/throw.ts:
```typescript
export function generateThrow(): ThrowResult
```

From src/hooks/useYutThrowScene.ts (created in 04-02):
```typescript
export function useYutThrowScene(): {
  canvasRef: React.RefObject<HTMLCanvasElement | null>
  phase: 'idle' | 'launching' | 'settling' | 'revealing' | 'disposed'
  revealedResult: ThrowResult | null
  throwOnce: (result: ThrowResult) => Promise<void>
  resetReveal: () => void
}
```

Existing board layer:
```typescript
export function Board(): React.JSX.Element
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create result card and overlay shell around the 3D hook</name>
  <files>src/components/throw/ThrowResultCard.tsx, src/components/throw/YutThrowOverlay.tsx, src/components/throw/__tests__/YutThrowOverlay.test.tsx</files>
  <read_first>
    - src/hooks/useYutThrowScene.ts (actual hook API from 04-02)
    - src/app/globals.css (current theme variables)
    - .planning/phases/04-3d-yut-throwing/04-CONTEXT.md (large centered Korean result requirement)
    - .planning/phases/03-2d-board-rendering/03-UI-SPEC.md (board-area sizing baseline and accent color)
  </read_first>
  <behavior>
    - Test 1: overlay renders a canvas container when `open` is true
    - Test 2: overlay calls `throwOnce(result)` exactly once when opened with a ThrowResult
    - Test 3: overlay renders `도`, `개`, `걸`, `윷`, `모` through the result-card mapping
    - Test 4: overlay shows the result card only when hook phase reaches `'revealing'`
    - Test 5: overlay calls `onComplete` after revealing
  </behavior>
  <action>
    **RED phase:** create `src/components/throw/__tests__/YutThrowOverlay.test.tsx` and mock `@/hooks/useYutThrowScene`. Run `npx vitest run src/components/throw/__tests__/YutThrowOverlay.test.tsx -x` and confirm failure first.

    **GREEN phase:** create `src/components/throw/ThrowResultCard.tsx` with:
    - `'use client'`
    - `export const RESULT_LABELS = { do: '도', gae: '개', geol: '걸', yut: '윷', mo: '모' } as const`
    - `export function ThrowResultCard({ result }: { result: ThrowResult }): React.JSX.Element`
    - root classes: `absolute inset-0 flex items-center justify-center pointer-events-none`
    - text classes: `text-[clamp(2.25rem,8vw,3.5rem)] font-bold tracking-tight text-[#FFD700] drop-shadow-[0_4px_12px_rgba(26,26,46,0.35)]`

    Create `src/components/throw/YutThrowOverlay.tsx` with:
    - `'use client'`
    - props interface:
      ```typescript
      {
        open: boolean
        result: ThrowResult | null
        onComplete: () => void
      }
      ```
    - use `useYutThrowScene()`
    - when `open` becomes true and `result` is non-null, call `throwOnce(result)` once for that result instance
    - render root wrapper as `absolute inset-0 z-20`
    - render a translucent backdrop `bg-white/15 backdrop-blur-[2px]`
    - render canvas container centered in the board area with `rounded-3xl overflow-hidden`
    - when hook `phase === 'revealing'` and `revealedResult` exists, render `<ThrowResultCard result={revealedResult} />`
    - start a `700ms` timer when reveal begins and then call `onComplete`

    Add one short comment explaining why the one-shot `throwOnce` guard exists.

    Run the overlay test file and make it pass.
  </action>
  <verify>
    <automated>npx vitest run src/components/throw/__tests__/YutThrowOverlay.test.tsx -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep "RESULT_LABELS = { do: '도', gae: '개', geol: '걸', yut: '윷', mo: '모' }" src/components/throw/ThrowResultCard.tsx` returns a match
    - `grep 'text-\\[clamp(2.25rem,8vw,3.5rem)\\]' src/components/throw/ThrowResultCard.tsx` returns a match
    - `grep 'throwOnce(result)' src/components/throw/YutThrowOverlay.tsx` returns a match
    - `grep '700' src/components/throw/YutThrowOverlay.tsx` returns a match
    - `npx vitest run src/components/throw/__tests__/YutThrowOverlay.test.tsx -x` passes with 0 failures
  </acceptance_criteria>
  <done>The overlay owns the reveal flow, launches exactly one throw per open cycle, and displays a centered large Korean result card when the scene reaches reveal state.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create the board-backed demo screen and replace the placeholder page</name>
  <files>src/components/throw/ThrowDemo.tsx, src/components/throw/__tests__/ThrowDemo.test.tsx, src/app/page.tsx</files>
  <read_first>
    - src/components/board/Board.tsx (static board renderer)
    - src/lib/yut/throw.ts (generateThrow source of truth)
    - src/components/throw/YutThrowOverlay.tsx (Task 1 actual props)
    - src/app/page.tsx (current placeholder page)
    - .planning/phases/04-3d-yut-throwing/04-CONTEXT.md (overlay-over-board layout)
  </read_first>
  <behavior>
    - Test 1: demo renders the `던지기` button and static board
    - Test 2: clicking the button calls `generateThrow()` once and opens the overlay
    - Test 3: the button is disabled while a throw is active
    - Test 4: `onComplete` from the overlay closes the active throw state and re-enables the button
    - Test 5: the page default export renders the demo instead of the placeholder `<h1>`
  </behavior>
  <action>
    **RED phase:** create `src/components/throw/__tests__/ThrowDemo.test.tsx`. Mock `@/lib/yut/throw` to return `{ name: 'gae', steps: 2, grantsExtra: false }` and mock `@/components/throw/YutThrowOverlay` with a simple test double that exposes whether `open` is true and allows calling `onComplete`. Run `npx vitest run src/components/throw/__tests__/ThrowDemo.test.tsx -x` and confirm failure first.

    **GREEN phase:** create `src/components/throw/ThrowDemo.tsx` with:
    - `'use client'`
    - local state `activeResult: ThrowResult | null`
    - renders a centered card-like container with:
      - heading `윷 던지기 미리보기`
      - a board wrapper `relative w-full max-w-[500px]`
      - `<Board />` as the base layer
      - `<YutThrowOverlay open={activeResult !== null} result={activeResult} onComplete={() => setActiveResult(null)} />`
      - a button labeled `던지기`
    - button behavior:
      - on click, call `generateThrow()`
      - store the returned ThrowResult in `activeResult`
      - set `disabled={activeResult !== null}`
    - button classes must include `min-h-11 rounded-full bg-[#1A1A2E] px-6 text-white disabled:opacity-50`

    Update `src/app/page.tsx` to:
    - remain a client component
    - import `ThrowDemo` from `@/components/throw/ThrowDemo`
    - render `<ThrowDemo />` inside the page instead of the placeholder heading

    Run the demo test file and make it pass.
  </action>
  <verify>
    <automated>npx vitest run src/components/throw/__tests__/ThrowDemo.test.tsx -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep '윷 던지기 미리보기' src/components/throw/ThrowDemo.tsx` returns a match
    - `grep '던지기' src/components/throw/ThrowDemo.tsx` returns a match
    - `grep 'generateThrow()' src/components/throw/ThrowDemo.tsx` returns a match
    - `grep '<Board />' src/components/throw/ThrowDemo.tsx` returns a match
    - `grep 'disabled={activeResult !== null}' src/components/throw/ThrowDemo.tsx` returns a match
    - `grep 'ThrowDemo' src/app/page.tsx` returns a match
    - `npx vitest run src/components/throw/__tests__/ThrowDemo.test.tsx -x` passes with 0 failures
  </acceptance_criteria>
  <done>The app now exposes a board-backed Phase 4 harness where the user taps `던지기`, sees the overlay, and returns to a ready state for the next throw after completion.</done>
</task>

</tasks>

<verification>
1. `npx vitest run src/components/throw/__tests__/YutThrowOverlay.test.tsx -x` — overlay reveal tests pass
2. `npx vitest run src/components/throw/__tests__/ThrowDemo.test.tsx -x` — demo interaction tests pass
3. `npm test` — full suite passes with the new Phase 4 harness
4. Manual browser check: click `던지기` and confirm board remains visible under the overlay while the result label appears centered after settle
</verification>

<success_criteria>
- The page renders a usable Phase 4 throw demo instead of the placeholder heading
- A visible `던지기` button launches the overlay using `generateThrow()`
- The result reveal is large, centered, and in Korean
- Duplicate launches are blocked while a throw is active
- Overlay and demo tests pass
</success_criteria>
