---
phase: 03-2d-board-rendering
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/board/PieceToken.tsx
  - src/components/board/HomeZone.tsx
  - src/components/board/__tests__/PieceToken.test.tsx
  - src/components/board/__tests__/HomeZone.test.tsx
autonomous: true
requirements:
  - BOARD-02
  - BOARD-03

must_haves:
  truths:
    - "Player pieces render as blue circles, AI pieces render as red circles"
    - "Stacked pieces show a numeric badge (2, 3, etc.) at top-right of the token"
    - "Selectable pieces show a pulsing glow ring animation"
    - "Selected piece shows a gold selection ring"
    - "HOME pieces display in a team-colored zone below the board"
    - "Each piece token has a 44px+ touch target"
  artifacts:
    - path: "src/components/board/PieceToken.tsx"
      provides: "Team-colored SVG circle token with stack badge, glow ring, and selection states"
      exports: ["PieceToken", "TEAM_COLORS"]
    - path: "src/components/board/HomeZone.tsx"
      provides: "Off-board piece display area with team-colored zones"
      exports: ["HomeZone"]
  key_links:
    - from: "src/components/board/PieceToken.tsx"
      to: "src/lib/yut/types.ts"
      via: "import Team type"
      pattern: "import.*Team.*from.*@/lib/yut"
---

<objective>
Create piece token and HOME zone components for displaying team pieces on and off the board.

Purpose: Implement the visual identity for game pieces -- player (blue) and AI (red) tokens with stack count badges, selectable/selected states, and off-board HOME staging areas. These components are consumed by the Board component in Plan 03 when wiring interaction.

Output: `PieceToken.tsx` with team colors, stack badge, glow ring, and selection ring. `HomeZone.tsx` for off-board pieces. Unit tests for both.
</objective>

<execution_context>
@/Users/nonsleeper/Documents/Coding/260331_v1/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nonsleeper/Documents/Coding/260331_v1/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-2d-board-rendering/03-CONTEXT.md
@.planning/phases/03-2d-board-rendering/03-UI-SPEC.md

<interfaces>
<!-- Key types the executor needs. -->

From src/lib/yut/types.ts:
```typescript
export type Team = 'player' | 'ai'
export const HOME = -1
export const FINISH = -2
export interface PieceState {
  id: string
  team: Team
  position: PiecePosition
  stackedPieceIds: string[]
  stackedWith: string | null
}
```

UI-SPEC Team Colors:
```
Player fill: #42A5F5, stroke: #1565C0 (blue family)
AI fill: #EF5350, stroke: #C62828 (red family)
```

UI-SPEC Piece Token Sizing (viewBox units):
```
Token radius: 14
Glow ring radius (pulsing): 18 to 22
Selection ring radius: 20
Invisible hit area radius: 22
Stack badge circle radius: 8
Stack badge font size: 10
Stack badge offset: translate(+8.4, -8.4) from token center (TOKEN_RADIUS * 0.6)
```

UI-SPEC HOME Zone:
```
HOME zone label (player): "내 말"
HOME zone label (ai): "상대 말"
HOME zone background: #E3F2FD (player), #FFEBEE (ai)
Height: 48px
```

Motion mock location: src/__mocks__/motion/react.tsx (created in Plan 01)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create PieceToken component with team colors, stack badge, and selection states</name>
  <files>src/components/board/PieceToken.tsx, src/components/board/__tests__/PieceToken.test.tsx</files>
  <read_first>
    - src/lib/yut/types.ts (Team type definition)
    - src/__mocks__/motion/react.tsx (motion mock for testing -- created by Plan 01 Task 2)
    - .planning/phases/03-2d-board-rendering/03-UI-SPEC.md (Piece Token Sizing, Team Colors, Animation Contract sections)
    - .planning/phases/03-2d-board-rendering/03-RESEARCH.md (Pattern 5: Glow Ring, Pitfall 6: motion.g positioning)
    - src/__tests__/setup.ts (test setup with jest-dom matchers)
  </read_first>
  <behavior>
    - Test 1: "renders player token with blue fill (#42A5F5) and stroke (#1565C0)"
    - Test 2: "renders AI token with red fill (#EF5350) and stroke (#C62828)"
    - Test 3: "shows stack badge with count 2 when stackCount is 2"
    - Test 4: "does not show stack badge when stackCount is 1"
    - Test 5: "renders glow ring circle when isSelectable is true"
    - Test 6: "does not render glow ring when isSelectable is false"
    - Test 7: "renders selection ring when isSelected is true"
    - Test 8: "calls onSelect when invisible hit area is clicked"
    - Test 9: "invisible hit area has r=22 for 44px+ touch target"
    - Test 10: "does not render hit area when isSelectable is false"
  </behavior>
  <action>
    **RED phase:** Create `src/components/board/__tests__/PieceToken.test.tsx` with all 10 tests. Import PieceToken (will not exist yet). Use `@testing-library/react` render. For SVG testing, render PieceToken inside an `<svg>` wrapper. Query circles by fill/stroke attributes or use `data-testid` attributes. Run tests -- all must fail.

    **GREEN phase:** Create `src/components/board/PieceToken.tsx`:
    - `'use client'` directive
    - Interface `PieceTokenProps`:
      ```
      cx: number
      cy: number
      team: Team (import from '@/lib/yut/types')
      stackCount: number (1 = solo, 2+ = stacked)
      isSelectable: boolean
      isSelected: boolean
      onSelect: () => void
      ```
    - Export `const TEAM_COLORS: Record<Team, { fill: string; stroke: string }>`:
      ```
      player: { fill: '#42A5F5', stroke: '#1565C0' }
      ai: { fill: '#EF5350', stroke: '#C62828' }
      ```
    - Export `const TOKEN_RADIUS = 14`
    - Named export `function PieceToken({ cx, cy, team, stackCount, isSelectable, isSelected, onSelect }: PieceTokenProps): JSX.Element`
    - Use `<motion.g animate={{ x: cx, y: cy }} transition={{ type: 'spring', stiffness: 300, damping: 25 }}>` as wrapper to position the token group
    - Inside the group, all child elements use local coordinates (centered at 0,0):
      - Invisible hit area (only when isSelectable): `<circle r={22} fill="transparent" style={{ cursor: 'pointer' }} onClick={onSelect} data-testid="piece-hit-area" pointerEvents="all" />`
      - Glow ring (only when isSelectable): `<motion.circle r={18} fill="none" stroke={colors.stroke} strokeWidth={2} animate={{ r: [18, 22, 18], opacity: [0.3, 0.8, 0.3], strokeWidth: [2, 4, 2] }} transition={{ duration: 1.5, repeat: Infinity, ease: 'easeInOut' }} data-testid="glow-ring" />`
      - Selection ring (only when isSelected): `<motion.circle r={20} fill="none" stroke="#FFD700" strokeWidth={3} initial={{ scale: 0.8, opacity: 0 }} animate={{ scale: 1, opacity: 1 }} transition={{ duration: 0.2 }} data-testid="selection-ring" />`
      - Main token circle: `<circle r={TOKEN_RADIUS} fill={colors.fill} stroke={colors.stroke} strokeWidth={2} data-testid="piece-token" />`
      - Stack badge (only when stackCount > 1): `<g transform="translate(8.4, -8.4)">` containing `<circle r={8} fill="#FFF" stroke={colors.stroke} strokeWidth={1.5} />` and `<text textAnchor="middle" dominantBaseline="central" fontSize={10} fontWeight="bold" fill={colors.stroke} data-testid="stack-badge">{stackCount}</text>`
    - Add `whileTap={{ scale: 0.9 }}` on the outer `motion.g` when isSelectable
    - JSDoc on the function and interface

    Run tests -- all must pass.
  </action>
  <verify>
    <automated>npx vitest run src/components/board/__tests__/PieceToken.test.tsx -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep "'use client'" src/components/board/PieceToken.tsx` returns a match
    - `grep '#42A5F5' src/components/board/PieceToken.tsx` returns a match (player fill)
    - `grep '#EF5350' src/components/board/PieceToken.tsx` returns a match (AI fill)
    - `grep '#1565C0' src/components/board/PieceToken.tsx` returns a match (player stroke)
    - `grep '#C62828' src/components/board/PieceToken.tsx` returns a match (AI stroke)
    - `grep '#FFD700' src/components/board/PieceToken.tsx` returns a match (selection ring gold)
    - `grep 'data-testid="stack-badge"' src/components/board/PieceToken.tsx` returns a match
    - `grep 'data-testid="glow-ring"' src/components/board/PieceToken.tsx` returns a match
    - `grep 'data-testid="piece-hit-area"' src/components/board/PieceToken.tsx` returns a match
    - `grep 'r={22}' src/components/board/PieceToken.tsx` returns a match (44px+ hit area)
    - `grep "import.*Team.*from '@/lib/yut" src/components/board/PieceToken.tsx` returns a match
    - `npx vitest run src/components/board/__tests__/PieceToken.test.tsx -x` passes with 0 failures
  </acceptance_criteria>
  <done>PieceToken renders player (blue) and AI (red) tokens with correct fills/strokes, shows stack count badge when >1, displays glow ring for selectable pieces, gold ring for selected piece, invisible 44px+ hit area, all 10 tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create HomeZone component for off-board pieces</name>
  <files>src/components/board/HomeZone.tsx, src/components/board/__tests__/HomeZone.test.tsx</files>
  <read_first>
    - src/lib/yut/types.ts (PieceState, Team, HOME sentinel)
    - src/components/board/PieceToken.tsx (created in Task 1 -- read actual file for props interface)
    - .planning/phases/03-2d-board-rendering/03-UI-SPEC.md (HOME Zone Interaction, Layout Contract, Copywriting Contract)
    - src/__mocks__/motion/react.tsx (motion mock)
  </read_first>
  <behavior>
    - Test 1: "renders player HOME zone with label '내 말'"
    - Test 2: "renders AI HOME zone with label '상대 말'"
    - Test 3: "renders player HOME zone with background color #E3F2FD"
    - Test 4: "renders AI HOME zone with background color #FFEBEE"
    - Test 5: "displays correct number of HOME pieces for each team"
    - Test 6: "calls onPieceSelect when a selectable HOME piece is clicked"
    - Test 7: "HOME pieces with IDs in selectablePieceIds are marked selectable"
  </behavior>
  <action>
    **RED phase:** Create `src/components/board/__tests__/HomeZone.test.tsx` with all 7 tests. Run -- all must fail.

    **GREEN phase:** Create `src/components/board/HomeZone.tsx`:
    - `'use client'` directive
    - Interface `HomePiece`:
      ```
      id: string
      team: Team
      stackCount: number
      ```
    - Interface `HomeZoneProps`:
      ```
      playerHomePieces: HomePiece[]
      aiHomePieces: HomePiece[]
      selectablePieceIds: string[]
      selectedPieceId: string | null
      onPieceSelect: (pieceId: string) => void
      ```
    - Named export `function HomeZone({ playerHomePieces, aiHomePieces, selectablePieceIds, selectedPieceId, onPieceSelect }: HomeZoneProps): JSX.Element`
    - Render as a flex row (Tailwind: `flex gap-4 w-full max-w-[500px] mx-auto`), height 48px
    - Left half: player zone with bg `#E3F2FD`, rounded corners, label "내 말" (font-semibold text-xs), display player pieces as small SVGs containing PieceToken components at fixed positions (cx=20 for first, cx=50 for second, cy=20)
    - Right half: AI zone with bg `#FFEBEE`, same layout, label "상대 말"
    - Each piece token: isSelectable = selectablePieceIds.includes(piece.id), isSelected = selectedPieceId === piece.id, onSelect = () => onPieceSelect(piece.id)
    - Add `data-testid="home-zone-player"` and `data-testid="home-zone-ai"` on zone containers
    - JSDoc on the function and interfaces

    Run tests -- all must pass.
  </action>
  <verify>
    <automated>npx vitest run src/components/board/__tests__/HomeZone.test.tsx -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep "'use client'" src/components/board/HomeZone.tsx` returns a match
    - `grep '내 말' src/components/board/HomeZone.tsx` returns a match
    - `grep '상대 말' src/components/board/HomeZone.tsx` returns a match
    - `grep '#E3F2FD' src/components/board/HomeZone.tsx` returns a match (player bg)
    - `grep '#FFEBEE' src/components/board/HomeZone.tsx` returns a match (AI bg)
    - `grep 'data-testid="home-zone-player"' src/components/board/HomeZone.tsx` returns a match
    - `grep 'data-testid="home-zone-ai"' src/components/board/HomeZone.tsx` returns a match
    - `grep "import.*PieceToken" src/components/board/HomeZone.tsx` returns a match
    - `npx vitest run src/components/board/__tests__/HomeZone.test.tsx -x` passes with 0 failures
    - `npm test` passes with 0 failures
  </acceptance_criteria>
  <done>HomeZone renders two team-colored zones with correct labels, displays HOME pieces using PieceToken, marks pieces as selectable when their IDs are in selectablePieceIds, calls onPieceSelect on click, all 7 tests pass, full suite green.</done>
</task>

</tasks>

<verification>
1. `npx vitest run src/components/board/__tests__/PieceToken.test.tsx -x` -- piece token tests pass
2. `npx vitest run src/components/board/__tests__/HomeZone.test.tsx -x` -- HOME zone tests pass
3. `npm test` -- full test suite passes (no regressions)
4. PieceToken correctly uses Team type from @/lib/yut/types
</verification>

<success_criteria>
- PieceToken renders blue (player) and red (AI) circles with correct hex colors
- Stack badge shows count when >1, hidden when solo
- Glow ring pulses for selectable, gold ring for selected
- 44px+ invisible hit area on selectable tokens
- HomeZone renders two team zones with Korean labels and correct background colors
- All tests pass including existing Phase 1/2 tests
</success_criteria>

<output>
After completion, create `.planning/phases/03-2d-board-rendering/03-02-SUMMARY.md`
</output>
