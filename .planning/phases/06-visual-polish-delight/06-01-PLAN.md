---
phase: 06-visual-polish-delight
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/board/PieceToken.tsx
  - src/components/board/HomeZone.tsx
  - src/components/board/Board.tsx
  - src/components/board/__tests__/PieceToken.test.tsx
  - src/components/board/__tests__/HomeZone.test.tsx
  - src/components/board/__tests__/Board.test.tsx
autonomous: true
requirements:
  - VIS-01

must_haves:
  truths:
    - "Board pieces read as cute casual characters rather than plain geometric circles"
    - "Character styling preserves existing stack badges, selection rings, and 44px touch targets"
    - "HOME zones and on-board pieces share the same visual language instead of diverging into separate token styles"
  artifacts:
    - path: "src/components/board/PieceToken.tsx"
      provides: "Cute character piece renderer that still supports stack and selection states"
      exports: ["PieceToken", "TEAM_COLORS", "TOKEN_RADIUS"]
    - path: "src/components/board/HomeZone.tsx"
      provides: "HOME-zone presentation aligned with the new character token visuals"
      exports: ["HomeZone", "HomePiece"]
  key_links:
    - from: "src/components/board/Board.tsx"
      to: "src/components/board/PieceToken.tsx"
      via: "renders the new character token for both static and animating pieces"
      pattern: "PieceToken"
    - from: "src/components/board/HomeZone.tsx"
      to: "src/components/board/PieceToken.tsx"
      via: "reuses the same character token language in HOME"
      pattern: "PieceToken"
---

<objective>
Ship the first delight layer by turning the current plain game pieces into cute, casual character tokens without breaking board readability or interaction contracts.

Purpose: Phase 5 solved the full game loop. Phase 6 begins by upgrading the most visible repeated element in the entire game: the pieces themselves. This plan should improve emotional appeal while keeping the board geometry, highlights, and touch behavior stable.

Output: character-token visuals in `PieceToken.tsx`, HOME/board integration updates, and tests that lock the new visual contract.
</objective>

<execution_context>
@/Users/nonsleeper/.codex/get-shit-done/workflows/execute-plan.md
@/Users/nonsleeper/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-visual-polish-delight/06-RESEARCH.md
@.planning/phases/06-visual-polish-delight/06-VALIDATION.md
@.planning/phases/03-2d-board-rendering/03-UI-SPEC.md

<interfaces>
Current token boundary from `src/components/board/PieceToken.tsx`:
```typescript
export function PieceToken(props: {
  cx: number
  cy: number
  team: Team
  stackCount: number
  isSelectable: boolean
  isSelected: boolean
  onSelect: () => void
}): JSX.Element
```

Current board/home consumers:
```typescript
<PieceToken ... />
```

Locked constraints:
```text
- keep the current board geometry and token footprint
- preserve stack badge support
- preserve invisible hit area for mobile touch targets
- keep selection and destination clarity from Phase 3
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Redesign PieceToken as a cute SVG-native character token</name>
  <files>src/components/board/PieceToken.tsx, src/components/board/__tests__/PieceToken.test.tsx</files>
  <read_first>
    - src/components/board/PieceToken.tsx
    - src/components/board/__tests__/PieceToken.test.tsx
    - .planning/phases/03-2d-board-rendering/03-UI-SPEC.md
    - .planning/phases/06-visual-polish-delight/06-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: player and AI pieces still render distinct team-specific colors
    - Test 2: stack badge still appears when `stackCount > 1`
    - Test 3: selectable pieces still render the invisible hit area and glow ring
    - Test 4: character-only details (for example face/ear/crest accents) render so the token is no longer just a flat circle
    - Test 5: selection ring still renders on top of the new character art
  </behavior>
  <action>
    **RED phase:** extend `src/components/board/__tests__/PieceToken.test.tsx` with assertions that the new character token includes at least one dedicated decorative element per team, such as a face, ear, beak, ribbon, or crest marker. Run `npx vitest run src/components/board/__tests__/PieceToken.test.tsx -x` and confirm failure first.

    **GREEN phase:** update `src/components/board/PieceToken.tsx` so each token becomes a cute SVG-native character while keeping the existing component API. Implementation requirements:
    - preserve `TEAM_COLORS` and `TOKEN_RADIUS`
    - keep the invisible hit circle at `r={22}` when selectable
    - keep glow ring and gold selection ring behavior
    - replace the flat token body with layered SVG primitives that read as a character, not just a plain disc
    - keep the art inside the current token footprint so board spacing does not need to change
    - keep the stack badge visible above the character art

    Add one short comment above the decorative art block explaining that the character details must stay lightweight so board density remains readable on mobile.

    Run the token test file again and get green.
  </action>
  <verify>
    <automated>npx vitest run src/components/board/__tests__/PieceToken.test.tsx -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep 'piece-hit-area' src/components/board/PieceToken.tsx` returns a match
    - `grep 'stack-badge' src/components/board/PieceToken.tsx` returns a match
    - `grep 'selection-ring' src/components/board/PieceToken.tsx` returns a match
    - `npx vitest run src/components/board/__tests__/PieceToken.test.tsx -x` passes with 0 failures
  </acceptance_criteria>
  <done>The board token reads as a cute character while still preserving stack, selection, and touch behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Propagate the new character token language through Board and HOME</name>
  <files>src/components/board/HomeZone.tsx, src/components/board/Board.tsx, src/components/board/__tests__/HomeZone.test.tsx, src/components/board/__tests__/Board.test.tsx</files>
  <read_first>
    - src/components/board/HomeZone.tsx
    - src/components/board/Board.tsx
    - src/components/board/__tests__/HomeZone.test.tsx
    - src/components/board/__tests__/Board.test.tsx
    - src/components/board/PieceToken.tsx
    - .planning/phases/06-visual-polish-delight/06-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: HOME zones still render both teams and piece selection works
    - Test 2: Board still renders on-board pieces, selected state, and animating pieces with the updated token
    - Test 3: no change to board geometry or destination highlight behavior is required
  </behavior>
  <action>
    **RED phase:** extend `src/components/board/__tests__/HomeZone.test.tsx` and `src/components/board/__tests__/Board.test.tsx` so they continue to verify HOME/board rendering after the token redesign. Run:
    - `npx vitest run src/components/board/__tests__/HomeZone.test.tsx -x`
    - `npx vitest run src/components/board/__tests__/Board.test.tsx -x`
    Confirm failures first if token internals changed their assumptions.

    **GREEN phase:** update `src/components/board/HomeZone.tsx` and `src/components/board/Board.tsx` only as needed to support the new token styling cleanly. Acceptable changes include:
    - minor layout or spacing adjustments that keep the current board footprint intact
    - keeping animating-piece rendering visually consistent with the static token art
    - preserving readability of HOME labels against the new token visuals

    Do not re-layout the board or alter station coordinates in this task.

    Add one short comment near any spacing tweak explaining that the geometry stays frozen in Phase 6 and only the presentation is changing.

    Run both test files again and get green.
  </action>
  <verify>
    <automated>npx vitest run src/components/board/__tests__/HomeZone.test.tsx src/components/board/__tests__/Board.test.tsx -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep 'PieceToken' src/components/board/HomeZone.tsx` returns a match
    - `grep 'PieceToken' src/components/board/Board.tsx` returns a match
    - `npx vitest run src/components/board/__tests__/HomeZone.test.tsx src/components/board/__tests__/Board.test.tsx -x` passes with 0 failures
    - No changes are made to `src/lib/yut/boardCoords.ts`
  </acceptance_criteria>
  <done>The new character token style appears consistently in both HOME and on-board states without destabilizing board layout.</done>
</task>

</tasks>

<verify>
1. `npx vitest run src/components/board/__tests__/PieceToken.test.tsx -x`
2. `npx vitest run src/components/board/__tests__/HomeZone.test.tsx src/components/board/__tests__/Board.test.tsx -x`
3. `npm test`
</verify>

<acceptance>
- Pieces read as cute casual characters rather than plain geometric tokens
- Stack badges, selection rings, and touch targets still work
- HOME and board pieces share one consistent character visual language
</acceptance>
