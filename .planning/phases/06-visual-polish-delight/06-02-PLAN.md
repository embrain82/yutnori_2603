---
phase: 06-visual-polish-delight
plan: 02
type: execute
wave: 2
depends_on:
  - "06-01"
files_modified:
  - src/components/effects/VictoryConfetti.tsx
  - src/components/effects/DefeatEffect.tsx
  - src/components/effects/__tests__/VictoryConfetti.test.tsx
  - src/components/effects/__tests__/DefeatEffect.test.tsx
  - src/components/screens/ResultScreen.tsx
  - src/components/screens/__tests__/ResultScreen.test.tsx
  - src/components/Game.tsx
  - src/components/__tests__/Game.test.tsx
autonomous: true
requirements:
  - VIS-05
  - VIS-06

must_haves:
  truths:
    - "Victory triggers a noticeable but replay-safe confetti celebration"
    - "Defeat presentation feels encouraging and keeps replay obvious"
    - "Celebration and defeat effects remain UI-owned side effects, not new store transitions"
  artifacts:
    - path: "src/components/effects/VictoryConfetti.tsx"
      provides: "Once-only confetti bursts tied to the victory phase"
      exports: ["VictoryConfetti"]
    - path: "src/components/effects/DefeatEffect.tsx"
      provides: "Short defeat-state flourish that softens the loss presentation"
      exports: ["DefeatEffect"]
    - path: "src/components/screens/ResultScreen.tsx"
      provides: "Polished victory/defeat presentation with clear replay CTA"
      exports: ["ResultScreen"]
  key_links:
    - from: "src/components/Game.tsx"
      to: "src/components/effects/VictoryConfetti.tsx"
      via: "renders the celebration effect only during the result phase"
      pattern: "VictoryConfetti"
    - from: "src/components/Game.tsx"
      to: "src/components/effects/DefeatEffect.tsx"
      via: "wraps or renders defeat-state polish without touching store flow"
      pattern: "DefeatEffect"
---

<objective>
Finish the delight layer by making wins feel celebratory and losses feel warm, while preserving fast replay and the stable Phase 5 gameplay architecture.

Purpose: This plan turns the current functional result flow into an emotionally complete finish. It should add confetti for wins, gentle defeat treatment, and a more polished result screen without changing the already-correct bridge/store behavior.

Output: effect components, upgraded result presentation, and Game integration/tests.
</objective>

<execution_context>
@/Users/nonsleeper/.codex/get-shit-done/workflows/execute-plan.md
@/Users/nonsleeper/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-visual-polish-delight/06-RESEARCH.md
@.planning/phases/06-visual-polish-delight/06-VALIDATION.md
@.planning/phases/05-game-screens-integration/05-VERIFICATION.md

<references>
Proven reference implementations:
- `/Users/nonsleeper/Documents/Coding/260330_rps/src/components/effects/Confetti.tsx`
- `/Users/nonsleeper/Documents/Coding/260330_rps/src/components/effects/DefeatEffect.tsx`
- `/Users/nonsleeper/Documents/Coding/260330_rps/src/components/screens/ResultScreen.tsx`
</references>

<interfaces>
Current result boundary:
```typescript
export function ResultScreen(): React.JSX.Element
```

Current game orchestration boundary:
```typescript
export default function Game(): React.JSX.Element
```

Locked constraints:
```text
- do not change store completion or bridge timing semantics from Phase 5
- replay must stay obvious and immediately tappable
- celebration effects must clean up on unmount/phase change
- use existing dependencies only (motion, canvas-confetti, React)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add victory and defeat effect components with cleanup-safe lifecycles</name>
  <files>src/components/effects/VictoryConfetti.tsx, src/components/effects/DefeatEffect.tsx, src/components/effects/__tests__/VictoryConfetti.test.tsx, src/components/effects/__tests__/DefeatEffect.test.tsx</files>
  <read_first>
    - /Users/nonsleeper/Documents/Coding/260330_rps/src/components/effects/Confetti.tsx
    - /Users/nonsleeper/Documents/Coding/260330_rps/src/components/effects/DefeatEffect.tsx
    - src/components/Game.tsx
    - src/components/screens/ResultScreen.tsx
    - .planning/phases/06-visual-polish-delight/06-RESEARCH.md
    - .planning/codebase/CONCERNS.md
  </read_first>
  <behavior>
    - Test 1: `VictoryConfetti` fires bursts exactly once when phase enters `victory`
    - Test 2: `VictoryConfetti` resets so a replayed win can fire again
    - Test 3: `DefeatEffect` triggers only when the game is in `defeat`
    - Test 4: timers and side effects are cleaned up on unmount
  </behavior>
  <action>
    **RED phase:** create `src/components/effects/__tests__/VictoryConfetti.test.tsx` and `src/components/effects/__tests__/DefeatEffect.test.tsx`. Mock `canvas-confetti`, `motion/react`, and the store selectors as needed. Run:
    - `npx vitest run src/components/effects/__tests__/VictoryConfetti.test.tsx -x`
    - `npx vitest run src/components/effects/__tests__/DefeatEffect.test.tsx -x`
    Confirm failures first.

    **GREEN phase:** add `VictoryConfetti.tsx` and `DefeatEffect.tsx` with these rules:
    - `VictoryConfetti`:
      - `'use client'`
      - reacts to `phase === 'victory'`
      - uses `canvas-confetti`
      - includes once-only ref and timer cleanup
      - uses `disableForReducedMotion: true`
      - resets correctly when leaving victory
    - `DefeatEffect`:
      - `'use client'`
      - uses `motion/react` imperative or declarative effect
      - keeps motion brief and non-blocking
      - suitable for wrapping or accompanying the defeat result state

    Add one short comment in `VictoryConfetti.tsx` explaining that replay safety matters because the result screen can re-enter victory multiple times across one session.

    Run both effect test files again and get green.
  </action>
  <verify>
    <automated>npx vitest run src/components/effects/__tests__/VictoryConfetti.test.tsx src/components/effects/__tests__/DefeatEffect.test.tsx -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep 'canvas-confetti' src/components/effects/VictoryConfetti.tsx` returns a match
    - `grep 'disableForReducedMotion' src/components/effects/VictoryConfetti.tsx` returns a match
    - `grep 'defeat' src/components/effects/DefeatEffect.tsx` returns a match
    - `npx vitest run src/components/effects/__tests__/VictoryConfetti.test.tsx src/components/effects/__tests__/DefeatEffect.test.tsx -x` passes with 0 failures
  </acceptance_criteria>
  <done>Victory and defeat now have dedicated cleanup-safe effect components ready for Game integration.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Polish ResultScreen and attach celebration/defeat effects in Game</name>
  <files>src/components/screens/ResultScreen.tsx, src/components/screens/__tests__/ResultScreen.test.tsx, src/components/Game.tsx, src/components/__tests__/Game.test.tsx</files>
  <read_first>
    - src/components/screens/ResultScreen.tsx
    - src/components/screens/__tests__/ResultScreen.test.tsx
    - src/components/Game.tsx
    - src/components/__tests__/Game.test.tsx
    - src/components/effects/VictoryConfetti.tsx
    - src/components/effects/DefeatEffect.tsx
    - .planning/phases/06-visual-polish-delight/06-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: victory result state renders a more celebratory presentation without losing coupon/replay behavior
    - Test 2: defeat result state renders encouraging copy and a clear retry CTA
    - Test 3: `Game` renders or wires `VictoryConfetti` during victory
    - Test 4: `Game` renders or wires `DefeatEffect` during defeat
  </behavior>
  <action>
    **RED phase:** extend `src/components/screens/__tests__/ResultScreen.test.tsx` and `src/components/__tests__/Game.test.tsx` for the new celebration/defeat integrations. Run:
    - `npx vitest run src/components/screens/__tests__/ResultScreen.test.tsx -x`
    - `npx vitest run src/components/__tests__/Game.test.tsx -x`
    Confirm failures first.

    **GREEN phase:** update `ResultScreen.tsx` and `Game.tsx` so that:
    - victory gets richer celebratory hierarchy and keeps coupon rendering intact
    - defeat gets warmer, more encouraging copy than the current minimal line
    - replay CTA remains prominent and unchanged in intent
    - `Game.tsx` integrates `VictoryConfetti` and defeat polish without changing the existing phase routing semantics

    Acceptable implementations:
    - wrapping defeat result content in `DefeatEffect`
    - rendering `VictoryConfetti` alongside `ResultScreen` when `phase === 'victory'`
    - adding small decorative motion to the result card entrance if it stays readable

    Add one short comment above the result-phase integration in `Game.tsx` explaining that Phase 6 effects decorate the verified Phase 5 result flow rather than altering it.

    Run both test files again and get green.
  </action>
  <verify>
    <automated>npx vitest run src/components/screens/__tests__/ResultScreen.test.tsx src/components/__tests__/Game.test.tsx -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep 'VictoryConfetti' src/components/Game.tsx` returns a match
    - `grep 'DefeatEffect' src/components/Game.tsx` returns a match
    - `grep '다시 도전' src/components/screens/ResultScreen.tsx` returns a match
    - `npx vitest run src/components/screens/__tests__/ResultScreen.test.tsx src/components/__tests__/Game.test.tsx -x` passes with 0 failures
    - Existing coupon area behavior remains in `ResultScreen`
  </acceptance_criteria>
  <done>The final result experience feels celebratory on wins and encouraging on losses without regressing replay or coupon behavior.</done>
</task>

</tasks>

<verify>
1. `npx vitest run src/components/effects/__tests__/VictoryConfetti.test.tsx src/components/effects/__tests__/DefeatEffect.test.tsx -x`
2. `npx vitest run src/components/screens/__tests__/ResultScreen.test.tsx src/components/__tests__/Game.test.tsx -x`
3. `npm test`
</verify>

<acceptance>
- Victory visibly celebrates with replay-safe confetti and result polish
- Defeat presentation feels encouraging and keeps restart obvious
- Store and bridge semantics from Phase 5 remain untouched
</acceptance>
